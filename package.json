{
  "name": "frigidereieftine.ro",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "node scripts/fetch-rating.mjs && node scripts/generate-redirects.mjs && astro build",
    "preview": "astro preview",
    "deploy": "npm run build && wrangler pages deploy dist",
    "download-images": "node scripts/download-images.mjs",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^5.7.10",
    "@astrojs/sitemap": "^3.3.1"
  },
  "devDependencies": {
    "wrangler": "^4.14.4"
  }
}
